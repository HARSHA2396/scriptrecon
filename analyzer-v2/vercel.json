{
    "version": 2,
    "public": true,
    "buildCommand": "npm install && npm run build --workspace=packages/core && npm run build --workspace=packages/web",
    "outputDirectory": "packages/web/dist",
    "installCommand": "npm install",
    "framework": null,
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/index.html"
        }
    ]
}